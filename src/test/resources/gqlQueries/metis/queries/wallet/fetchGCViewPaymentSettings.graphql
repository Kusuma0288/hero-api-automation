query fetchGCPaymentSettings {
    paymentSettings {
        description
        canAddSchemeCard
        items {
            ... on GiftCardPaymentSetting {
                logoURL
                name
                useFirst
                disabled
            }
        }
    }
}